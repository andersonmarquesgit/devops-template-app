apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-template-app.fullname" . }}
  labels:
    {{- include "devops-template-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-template-app.selectorLabels" . | nindent 4 }}
